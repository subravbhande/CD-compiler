#include <iostream>
#include <math.h>

using namespace std;

int main() {

    double base = 2;
    double exponent = 5;

    double result = pow(base, exponent);

    cout << "Power = "
         << result
         << endl;

    return 0;
}